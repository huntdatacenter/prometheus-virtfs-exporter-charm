name: prometheus-virtfs-exporter
summary: Prometheus VirtFS Exporter
maintainer: Matus Kosut <matus.kosut@ntnu.no>
description: |
  Exporting VirtFS disk metrics for Prometheus.
tags:
  - monitoring
  - prometheus
subordinate: true
series:
  - xenial
  - bionic
requires:
  nova-compute:
    interface: juju-info
    scope: container
provides:
  prometheus-target:
    interface: http
  prometheus-manual-job:
    interface: prometheus-manual
